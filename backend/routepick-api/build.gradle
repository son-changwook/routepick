plugins {
    id 'java'
    id 'org.springframework.boot' version '3.2.3'
    id 'io.spring.dependency-management' version '1.1.3'
}

group = 'com.routepick'
version = '0.0.1-SNAPSHOT'

java {
    sourceCompatibility = '17'
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // WebSocket 의존성 (api 모듈만의 특수 의존성)
    implementation 'org.springframework.boot:spring-boot-starter-websocket'
    
    // JWT 관련 의존성 (api 모듈만의 특수 의존성)
    implementation 'io.jsonwebtoken:jjwt-api:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.11.5'
    
    // MyBatis 테스트 의존성 (api 모듈만의 특수 의존성)
    testImplementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter-test:3.0.3'

    implementation project(':routepick-common')

    // 버킷 4j 의존성 (api 모듈만의 특수 의존성)
    implementation 'com.bucket4j:bucket4j-core:8.5.0'
    implementation 'com.bucket4j:bucket4j-caffeine:8.5.0'

    // 이메일 발송을 위한 의존성
    implementation 'org.springframework.boot:spring-boot-starter-mail'
    
    // Swagger/OpenAPI 의존성
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.2.0'
}

bootRun {
    mainClass = 'com.routepick.api.ApiApplication'
}

bootJar {
    mainClass = 'com.routepick.api.ApiApplication'
}

tasks.named('test') {
    useJUnitPlatform()
}
